import './shapes.css';

export const Three = ({
  isHover,
  isPress,
  mouseX,
  mouseY,
}: {
  isHover: boolean;
  isPress: boolean;
  mouseX: number;
  mouseY: number;
}) => {
  return <div>Shapes go here</div>;
};
